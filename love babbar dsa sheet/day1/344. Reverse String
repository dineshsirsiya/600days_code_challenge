class Solution {
    public void reverseString(char[] s) {
        int len=s.length;
        int n=s.length-1;
        int i=0;
        while(i<len/2){
            char temp=s[i];
            s[i]=s[n];
            s[n]=temp;
            i++;
            n--;
        }
        
    }
}
